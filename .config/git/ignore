.clj-kondo/
.cpcache/
.lsp/
.idea/

.DS_Store

# for WSL file metadata
*:Zone.Identifier

# Generated theme files
.config/alacritty/generated-theme.toml
.config/helix/themes/generated.toml
.config/zellij/themes/generated.kdl
/generated-ls-colors.zsh
.current-theme

# Claude per-repo config
.claude/
**/.claude/settings.local.json

# fk per-repo local state (NOT ~/.fk/, which is per-machine global config —
# this is the relative ./.fk/ that may appear inside any repo we work in)
.fk/
