
This repo expects to live at ~/dotfiles
Don't install these dotfiles.

Instructions
------------

Prereqs: curl, git, zsh, nvim, tmux

Install:

	cd
	git clone git@github.com:alexandercampbell/dotfiles.git
	ln -s dotfiles/.config
	ln -s dotfiles/.vimrc
	ln -s dotfiles/.tmux.conf
	ln -s dotfiles/.zshrc
	ln -s dotfiles/.gitconfig
	ln -s dotfiles/.spacemacs

	cd dotfiles/scripts
	./install_zplug.sh
	./install_vim-plug.sh
	exec zsh

